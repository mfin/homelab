---
name: "Hello World"

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  hello:
    name: Hello World
    runs-on: self-hosted
    steps:
      - name: Output hello text
        run: |
          sleep 120 && echo "HELLO WORLD"
