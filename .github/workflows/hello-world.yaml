---
name: "Hello World"

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  hello:
    name: Hello World
    runs-on: self-hosted
    steps:
      - name: Output hello text
        run: |
          echo "HELLO WORLD"
