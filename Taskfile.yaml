---
version: "3"

vars:
  ANSIBLE_DIR: "{{.ROOT_DIR}}/ansible"
  CLUSTER_DIR: "{{.ROOT_DIR}}/k8s"

includes:
  an: .taskfiles/ansible/Tasks.yaml

tasks:
  default:
    silent: true
    cmds: ["task -l"]
