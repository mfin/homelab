---
version: '3'

tasks:
  secrets:
    desc: Decrypt secrets in current directory
    dir: '{{.USER_WORKING_DIR}}'
    silent: true
    cmds:
      - find . -type f \( -name "*.sops.yaml" -o -name "secrets.yaml" \) -exec sops -d {} \;
