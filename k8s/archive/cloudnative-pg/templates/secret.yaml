apiVersion: v1
kind: Secret
metadata:
  name: cloudnative-pg
type: Opaque
stringData:
  POSTGRES_SUPER_USER: "{{ .Values.postgres.superUser }}"
  POSTGRES_SUPER_PASS: "{{ .Values.postgres.superPass }}"
