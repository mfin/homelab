app-template:
  controller:
    replicas: 1
    type: statefulset
  image:
    repository: ghcr.io/jmmaloney4/roon
    tag: 2.0.1259
  env:
    TZ: Europe/Ljubljana
  hostNetwork: true
  service:
    main:
      enabled: false
  ingress:
    main:
      enabled: false
  podSecurityContext:
    fsGroup: 100
    fsGroupChangePolicy: "OnRootMismatch"
  persistence:
    config:
      enabled: true
      existingClaim: roon
      mountPath: /var/roon
    music:
      enabled: true
      type: nfs
      path: /volume1/data/music
      mountPath: /music
  resources:
    requests:
      cpu: 10m
      memory: 250Mi
    limits:
      memory: 2000Mi
