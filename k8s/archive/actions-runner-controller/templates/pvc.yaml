apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actions-runner-controller-cache
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 10Gi
