redis-paperless:
  fullnameOverride: redis-paperless
  labels:
    app.kubernetes.io/deployment: redis-paperless
  controllers:
    main:
      containers:
        main:
          image:
            repository: docker.io/library/redis
            tag: 7.2.3
          resources:
            requests:
              cpu: 23m
              memory: 64M
            limits:
              memory: 64M
  service:
    main:
      extraSelectorLabels:
        app.kubernetes.io/deployment: redis-paperless
      ports:
        http:
          enabled: false
        redis:
          enabled: true
          port: 6379

redis-authentik:
  fullnameOverride: redis-authentik
  labels:
    app.kubernetes.io/deployment: redis-authentik
  controllers:
    main:
      containers:
        main:
          image:
            repository: docker.io/library/redis
            tag: 7.2.3
          resources:
            requests:
              cpu: 23m
              memory: 64M
            limits:
              memory: 64M
  service:
    main:
      extraSelectorLabels:
        app.kubernetes.io/deployment: redis-authentik
      ports:
        http:
          enabled: false
        redis:
          enabled: true
          port: 6379
