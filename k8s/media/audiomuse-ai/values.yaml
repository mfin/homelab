app-template:
  enabled: false
  controllers:
    server:
      replicas: 0
      type: statefulset
      containers:
        server:
          image:
            repository: ghcr.io/neptunehub/audiomuse-ai
            tag: 2.6.2@sha256:f9c4df0ce93a7a4bcd69d5bcd15a5cc4fdb13b37a47f84773d8a24fcd0ec2a0e
          env:
            SERVICE_TYPE: flask
          envFrom:
            - configMapRef:
                name: audiomuse-config
            - secretRef:
                name: audiomuse-secret
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: false
            startup:
              enabled: false
          resources:
            requests:
              cpu: 10m
              memory: 1Gi
            limits:
              memory: 6Gi
      pod:
        annotations:
          reloader.stakater.com/auto: "true"

    worker:
      replicas: 0
      containers:
        worker:
          image:
            repository: ghcr.io/neptunehub/audiomuse-ai
            tag: 2.6.2@sha256:f9c4df0ce93a7a4bcd69d5bcd15a5cc4fdb13b37a47f84773d8a24fcd0ec2a0e
          env:
            SERVICE_TYPE: worker
          envFrom:
            - configMapRef:
                name: audiomuse-config
            - secretRef:
                name: audiomuse-secret
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: false
            startup:
              enabled: false
          resources:
            # claims:
            #   - name: gpu
            requests:
              cpu: 10m
              memory: 1Gi
            limits:
              memory: 2Gi
      pod:
        annotations:
          reloader.stakater.com/auto: "true"
        # resourceClaims:
        #   - name: gpu
        #     resourceClaimTemplateName: audiomuse-ai

  service:
    server:
      controller: server
      ports:
        http:
          port: 8000

  route:
    server:
      hostnames:
        - audiomuse.mfin.app
      parentRefs:
        - name: envoy-internal
          namespace: envoy-gateway
      rules:
        - backendRefs:
            - identifier: server
              port: 8000

  persistence:
    tmp:
      type: emptyDir
      globalMounts:
        - path: /app/temp_audio
