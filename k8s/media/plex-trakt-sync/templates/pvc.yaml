apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-trakt-sync
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 30Gi
