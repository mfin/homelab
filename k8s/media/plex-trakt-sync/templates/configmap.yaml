apiVersion: v1
kind: ConfigMap
metadata:
  name: plex-trakt-sync
data:
  config.yml: |-
    sync:
      plex_to_trakt:
        clear_collected: true
        watchlist: false
      trakt_to_plex:
        liked_lists: false
        watchlist: false

    watch:
      add_collection: false
