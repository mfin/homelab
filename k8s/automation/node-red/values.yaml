node-red:
  env:
    TZ: "Europe/Ljubljana"

  persistence:
    enabled: true
    existingClaim: node-red-pvc

  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - host: &host node-red.lab.finzgar.net
    tls:
      hosts:
        - *host

    # annotations:
    #   traefik.ingress.kubernetes.io/router.entrypoints: websecure
    #   traefik.ingress.kubernetes.io/router.tls: "true"
