apiVersion: v1
kind: ConfigMap
metadata:
  name: tika-config
data:
  tika-config.xml: |-
    <?xml version="1.0" encoding="UTF-8"?>
    <properties>
      <server>
        <params>
          <logLevel>debug</logLevel>
        </params>
      </server>
    </properties>
