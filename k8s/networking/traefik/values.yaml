traefik:
  additionalArguments:
    - "--providers.kubernetesingress.ingressendpoint.publishedservice=traefik/traefik"

  service:
    annotations:
      metallb.universe.tf/loadBalancerIPs: 10.10.0.5

  ports:
    web:
      redirectTo: websecure
