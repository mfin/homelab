---
kind: Service
apiVersion: v1
metadata:
  name: node-exporter-pi
  labels:
    external: node-exporter-pi
spec:
  type: ClusterIP
  ports:
  - name: metrics
    port: {{ .Values.nodeExporter.pi.port }}
    targetPort: {{ .Values.nodeExporter.pi.port }}
---
kind: Endpoints
apiVersion: v1
metadata:
  name: node-exporter-pi
subsets:
  - addresses:
      - ip: {{ .Values.nodeExporter.pi.ip }}
    ports:
      - name: metrics
        port: {{ .Values.nodeExporter.pi.port }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
  name: node-exporter-pi
spec:
  endpoints:
  - path: /metrics
    port: metrics
    scheme: http
  selector:
    matchLabels:
      external: node-exporter-pi
