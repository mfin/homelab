---
kind: Service
apiVersion: v1
metadata:
  name: node-exporter-nas
  labels:
    external: node-exporter-nas
spec:
  type: ClusterIP
  ports:
  - name: metrics
    port: {{ .Values.nodeExporter.nas.port }}
    targetPort: {{ .Values.nodeExporter.nas.port }}
---
kind: Endpoints
apiVersion: v1
metadata:
  name: node-exporter-nas
subsets:
  - addresses:
      - ip: {{ .Values.nodeExporter.nas.ip }}
    ports:
      - name: metrics
        port: {{ .Values.nodeExporter.nas.port }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
  name: node-exporter-nas
spec:
  endpoints:
  - path: /metrics
    port: metrics
    scheme: http
  selector:
    matchLabels:
      external: node-exporter-nas
