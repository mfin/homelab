apiVersion: v1
kind: Secret
metadata:
  name: dns-internal-secret
type: Opaque
stringData:
  rndc-zone: {{ .Values.bind.zone }}
  rndc-algorithm: {{ .Values.bind.algorithm }}
  rndc-secret: {{ .Values.bind.secret }}
