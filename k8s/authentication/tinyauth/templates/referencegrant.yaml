apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-extauth
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
  to:
    - group: ""
      kind: Service
      name: tinyauth
