app-template:
  controllers:
    main:
      type: statefulset
      replicas: 1
      containers:
        main:
          image:
            repository: linuxserver/pairdrop
            tag: latest@sha256:85806e7a748748802cce3f32115fb735bb7b7bced63c69b8667121f658a42d1f
          env:
            PUID: 1000
            PGID: 1000
            TZ: Europe/Ljubljana
      pod:
        securityContext:
          fsGroup: 1000
          fsGroupChangePolicy: "OnRootMismatch"
  service:
    main:
      ports:
        http:
          port: 3000
  ingress:
    main:
      enabled: true
      className: external
      annotations:
        hajimari.io/appName: "PairDrop"
        hajimari.io/icon: water
        hajimari.io/enable: "true"
        hajimari.io/info: Local file sharing in your browser. Inspired by Apple's AirDrop.
  persistence:
    config:
      enabled: true
      existingClaim: pairdrop
      globalMounts:
        - path: /config
